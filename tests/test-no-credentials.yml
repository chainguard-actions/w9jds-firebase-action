name: Test no credentials error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        # No FIREBASE_TOKEN, GCP_SA_KEY, or GOOGLE_APPLICATION_CREDENTIALS set.
        # The entrypoint.sh exits with code 126 when no credentials are provided.
      - name: Assert action failed due to missing credentials
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action correctly failed when no credentials were provided"
          else
            echo "FAIL: expected action to fail without credentials, got outcome '$OUTCOME'"
            exit 1
          fi
