name: Test firebase help command with token
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up git origin reachable inside Docker container
        run: |
          git config user.email "test@example.com"
          git config user.name "Test User"
          rm -rf "$GITHUB_WORKSPACE/.git-remote"
          git init --bare "$GITHUB_WORKSPACE/.git-remote"
          git push "$GITHUB_WORKSPACE/.git-remote" HEAD:refs/heads/act-harness
          git remote set-url origin "$GITHUB_WORKSPACE/.git-remote" 2>/dev/null || \
            git remote add origin "$GITHUB_WORKSPACE/.git-remote"

      - id: action
        uses: .
        with:
          args: help
        env:
          FIREBASE_TOKEN: fake-ci-token-for-testing
        continue-on-error: true

      - name: Assert action ran past credential check
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # With FIREBASE_TOKEN set, the action passes the credential check.
          # 'firebase help' may succeed or fail depending on the token validity,
          # but it must NOT fail with exit 126 (missing credentials).
          # We accept both success (help printed) and failure (invalid token error from firebase),
          # but not a missing-credentials exit.
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: firebase help ran successfully with FIREBASE_TOKEN set"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action ran past credential check (firebase may have rejected the fake token)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
